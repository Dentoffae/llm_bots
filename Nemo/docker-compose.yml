services:
  nemo:
    build: .
    image: nemo-bot
    container_name: nemo-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/nemo.db
    volumes:
      - nemo-data:/data

volumes:
  nemo-data:
